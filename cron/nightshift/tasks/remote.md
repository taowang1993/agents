# TockDriver Remote Agent and Device Routing

1. Read `.agents/reference/remote.md`.
2. Review TockDriver remote agent and device routing and fix bad code.
3. Update `.agents/reference/remote.md` if needed.
4. Validate, commit, and push; exit nonzero if blocked.

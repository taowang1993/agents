# TockDriver Memory System

1. Read `.agents/reference/memory.md`.
2. Review the memory system and fix bad code.
3. Update `.agents/reference/memory.md` if needed.
4. Validate, commit, and push; exit nonzero if blocked.

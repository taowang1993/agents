# Sync

1. Read `.agents/reference/sync.md`.
2. Review Local-First Sync and Offline Reliability and fix bad code.
3. Update `.agents/reference/sync.md` if needed.
4. Validate, commit, and push; exit nonzero if blocked.

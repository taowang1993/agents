# TockDriver Core Runtime

1. Read `.agents/reference/architecture.md` and `.agents/reference/observability.md`.
2. Review the TockDriver core runtime and fix bad code.
3. Update `.agents/reference/architecture.md` and `.agents/reference/observability.md` if needed.
4. Validate, commit, and push; exit nonzero if blocked.

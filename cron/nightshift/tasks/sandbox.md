# TockDriver Secure Workspace and Tool Authorization

1. Read `.agents/reference/sandbox.md`.
2. Review TockDriver secure workspace and tool authorization and fix bad code.
3. Update `.agents/reference/sandbox.md` if needed.
4. Validate, commit, and push; exit nonzero if blocked.

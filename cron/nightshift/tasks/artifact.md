# TockDriver Artifact

1. Read `.agents/reference/artifact.md`.
2. Review TockDriver artifacts and fix bad code.
3. Update `.agents/reference/artifact.md` if needed.
4. Validate, commit, and push; exit nonzero if blocked.

---
description: TockDesigner
---

## Task 1: Parity

Bring TockDesigner to feature parity with the vendor Open Design.

1. Read `.agents/reference/tockdesigner.md` and use the `od-audit` skill.

2. Compare TockDesigner against the pinned `vendor/open-design`. 

- Focus on feature, functional, behavioral, contract, and runtime gaps. 
- Ignore visual-only styling differences when they follow the web design rules in `.pi/rules/web.md`.

3. Close the gaps. Update TockDesigner parity/compatibility artifacts when behavior or contracts change.


## Task 2: Review

1. Review TockDesigner. Fix bad code if any. 

2. Run TockDesigner unit, typecheck, parity, compatibility, IPC, env, and quality gates.

3. Update `.agents/reference/tockdesigner.md` if needed. Then, commit and push. 
